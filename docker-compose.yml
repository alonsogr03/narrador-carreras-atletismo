services:
  # 1. EL BROKER DE MENSAJERÍA
  kafka:
    image: apache/kafka:4.1.1
    container_name: cluster_kafka
    ports:
      - "9092:9092"

    environment:
      KAFKA_NODE_ID: 1
      KAFKA_PROCESS_ROLES: broker,controller

      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:29092,CONTROLLER://0.0.0.0:29093,PLAINTEXT_HOST://0.0.0.0:9092

      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:29092,PLAINTEXT_HOST://localhost:9092

      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: CONTROLLER:PLAINTEXT,PLAINTEXT:PLAINTEXT,PLAINTEXT_HOST:PLAINTEXT

      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT

      KAFKA_CONTROLLER_LISTENER_NAMES: CONTROLLER

      KAFKA_CONTROLLER_QUORUM_VOTERS: 1@kafka:29093

      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1

  workspace:
    build: .
    container_name: dev_workspace
    ports:
      - "8888:8888" # Acceso al IDE (JupyterLab)
      - "8501:8501" # Acceso a la web de Streamlit
    environment:
      # Variables de entorno accesibles para tus scripts
      - KAFKA_BROKER=kafka:29092
    volumes:
      - ./:/home/jovyan/work # Mapea tu carpeta local en tiempo real
    command: start-notebook.py --NotebookApp.token='' --NotebookApp.password=''
    depends_on:
      - kafka